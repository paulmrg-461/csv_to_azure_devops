ORGANIZATION=
PROJECT=
AZURE_DEVOPS_PAT=
DEEPSEEK_API_KEY=
# Archivo de entrada: .csv o .xlsx (detectado por extensión)
INPUT_FILE=tasks.csv
# Separador CSV (por defecto TAB). Para comas: CSV_SEPARATOR=,
# CSV_SEPARATOR=,
